# Copy this file to .env before running the app

# Port the server listens on
PORT=5000

# Secret used to sign JWT auth tokens — change this to a long random string in production
JWT_SECRET=replace_this_with_a_long_random_secret_string
JWT_EXPIRES_IN=7d

# Set to "production" when deploying
NODE_ENV=development

# ---- Amazon RDS (MySQL) connection settings ----
# Found on your RDS instance's "Connectivity & security" tab in the AWS console.
DB_HOST=your-db-instance.xxxxxxxxxxxx.us-east-1.rds.amazonaws.com
DB_PORT=3306
DB_USER=admin
DB_PASSWORD=your_db_password
DB_NAME=pulse_chat

# Set to "true" to connect over SSL (recommended; required by some RDS configurations)
DB_SSL=true

# ---- Redis (required only if you run more than one instance/process) ----
# Needed for: cross-instance message delivery, and shared online/offline presence.
# Example (Amazon ElastiCache): redis://your-cluster.xxxxxx.ng.0001.use1.cache.amazonaws.com:6379
# Leave unset for local dev or a single-instance deployment.
REDIS_URL=
REDIS_TLS=false
